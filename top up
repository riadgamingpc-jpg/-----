rx-topup/
│
├── server.js
├── package.json
│
├── models/
│   ├── User.js
│   └── Order.js
│
├── routes/
│   ├── auth.js
│   └── order.js
│const mongoose = require("mongoose");

const OrderSchema = new mongoose.Schema({
  userId: String,
  package: String,
  uid: String,
  paymentNumber: String,
  status: { type: String, default: "pending" }
});

module.exports = mongoose.model("Order", OrderSchema);
└── frontend/
    ├── index.html
    ├── login.html
    ├── admin.html
    └── app.jsnpm init -y
npm install express mongoose cors bcrypt const express = require("express");
const mongoose = require("mongoose");
const cors = require("cors");

const app = express();
app.use(cors());
app.use(express.json());

mongoose.connect("mongodb://127.0.0.1:27017/rxtopup");

app.use("/api/auth", require("./routes/auth"));
app.use("/api/orders", require("./routes/order"));
const mongoose = require("mongoose");

const UserSchema = new mongoose.Schema({
  username: String,
  password: String,
  role: { type: String, default: "user" }
});

module.exports = mongoose.model("User", UserSchema);
app.listen(5000, () => console.log("Server running const router = require("express").Router();
const User = require("../models/User");
const bcrypt = require("bcrypt");
const jwt = require("jsonwebtoken");

router.post("/register", async (req,res)=>{
  const hash = await bcrypt.hash(req.body.password,10);

  const user = new User({
    username:req.body.username,
    password:hash
  });

  await user.save();
  res.send("User Created");
});

router.post("/login", async (req,res)=>{
  const user = await User.findOne({username:req.body.username});
  if(!user) return res.send("User Not Found");

  const ok = await bcrypt.compare(req.body.password,user.password);
  if(!ok) return res.send("Wrong Password");

  const token = jwt.sign({id:user._id},"SECRET");
  res.json({token});
});

module.exports = router; 5000"));const router = require("express").Router();
const Order = require("../models/Order");
const jwt = require("jsonwebtoken");

router.post("/create", async (req,res)=>{
  const token = req.headers.token;
  const user = jwt.verify(token,"SECRET");

  const order = new Order({
    userId: user.id,
    package: req.body.package,
    uid: req.body.uid,
    paymentNumber: req.body.paymentNumber
  });

  await order.save();
  res.send("Order Placed");
});

router.get("/", async (req,res)=>{
  const orders = await Order.find();
  res.json(orders);
});

module.exports = router;<h2>RX TOP UP</h2>

<input id="uid" placeholder="Game UID">
<input id="pack" readonly placeholder="Select Package">

<button onclick="selectPack('50 Diamond - 39 TK')">50 💎</button>
<button onclick="selectPack('115 Diamond - 75 TK')">115 💎</button>

<button onclick="placeOrder()">Order</button>

<script src="app.js"></script>function selectPack(p){
  document.getElementById("pack").value = p;
}

async function placeOrder(){
  const res = await fetch("http://localhost:5000/api/orders/create",{
    method:"POST",
    headers:{
      "Content-Type":"application/json",
      "token": localStorage.getItem("token")
    },
    body:JSON.stringify({
      package: document.getElementById("pack").value,
      uid: document.getElementById("uid").value,
      paymentNumber: "bKash/Nagad"
    })
  });

  alert(await res.text());<p>📱 bKash/Nagad Payment Number: <b>01851634841</b></p>
}body: JSON.stringify({
  package: document.getElementById("pack").value,
  uid: document.getElementById("uid").value,
  paymentNumber: "01851634841"
})<h2>Login</h2>

<input id="username" placeholder="Username">
<input id="password" type="password" placeholder="Password">

<button onclick="login()">Login</button>

<script>
async function login(){
  const res = await fetch("http://localhost:5000/api/auth/login",{
    method:"POST",
    headers:{"Content-Type":"application/json"},
    body:JSON.stringify({
      username:username.value,
      password:password.value
    })
  });

  const data = await res.json();
  localStorage.setItem("token",data.token);
  alert("Login Done");
}<h2>Admin Panel</h2>
<div id="orders"></div>

<script>
fetch("http://localhost:5000/api/orders")
.then(res=>res.json())
.then(data=>{
  document.getElementById("orders").innerHTML =
  data.map(o=>`
    <p>${o.package} - ${o.uid} - ${o.status}</p>
  `).join("");
});
</script>mongod
node server.git init
git add .
git commit -m "RX TOP UP FULL SYSTEM"
git branch -M main
git remote add origin https://github.com/your-username/rx-topup.git
git push -u origin main
</script>
